#include "h_compare.h"

double find_max(double a, double b) {
    if (a > b) {
        return a;
    } else {
        return b;
    };
}
double find_min(double a, double b) {
    if (a < b) {
        return a;
    } else {
        return b;
    };
}
