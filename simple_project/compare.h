#pragma once
#include "compare.cpp"

double find_max(double a, double b);
double find_min(double a, double b);

// header (.h) file
// define #pragma once to prevent duplicate include.
// then function from source cpp file